{
    "name": "djem",
    "private": true,
    "devDependencies": {
        "watch": "latest"
    },
    "scripts": {
        "lint": "isort -rc --diff . && flake8 . || true",
        "watch": "watch 'npm run lint' djem"
    }
}
